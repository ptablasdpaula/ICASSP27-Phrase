# Eight-direction gradient screen

The complete 510-configuration screen is running: all 255 nonempty subsets of
four diagonal and four orthogonal directions, each without and with Log-Weighing.
The frozen targets, candidates and scoring match the earlier diagonal screen.

See the [protocol](protocol.md). Results will be added after all shards and
legacy-reproduction checks pass. The paper has not been changed.
