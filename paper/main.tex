\documentclass[conference]{IEEEtran}
% Overleaf runs BibTeX from the project top level even when the selected main
% document lives in paper/.  Resolve one prefix for both a full-repository
% Overleaf project and a standalone upload of the paper/ directory.
\ifdefined\PaperRoot\else
  \IfFileExists{paper/references.bib}{%
    \newcommand{\PaperRoot}{paper}%
  }{%
    \newcommand{\PaperRoot}{.}%
  }%
\fi
% IEEE conference template version as of 6/27/2024.

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{textcomp}
\usepackage{url}
\usepackage{xcolor}
\usepackage[acronym,nonumberlist,nopostdot]{glossaries}
\makeglossaries
% Keep IEEE's typography and page geometry while avoiding oversized gaps
% around floats.
\setlength{\textfloatsep}{7pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{7pt plus 2pt minus 2pt}
\setlength{\dblfloatsep}{6pt plus 2pt minus 2pt}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\title{Gradient Descent Optimization of Plucked-String Musical Phrases via
Cumulative Energy Losses}
\author{\IEEEauthorblockN{Pablo Tablas de Paula\IEEEauthorrefmark{1},
Sebastian J. Schlecht\IEEEauthorrefmark{2},
Emmanouil Benetos\IEEEauthorrefmark{1}, and
Joshua D. Reiss\IEEEauthorrefmark{1}}
\IEEEauthorblockA{\IEEEauthorrefmark{1}Centre for Digital Music,
Queen Mary University of London, London, UK}
\IEEEauthorblockA{\IEEEauthorrefmark{2}Multimedia Communications and Signal Processing,\\
Friedrich-Alexander-Universit\"at Erlangen-N\"urnberg, Erlangen, Germany}
\IEEEauthorblockA{\texttt{p.tablasdepaula@qmul.ac.uk};
\texttt{sebastian.schlecht@fau.de}}}

% Expand acronyms on first use; the introduction starts a fresh first-use scope.
\newacronym{ddsp}{DDSP}{differentiable digital signal processing}
\newacronym{ot}{OT}{optimal transport}
\newacronym[
  shortplural={Ce\ensuremath{\mathcal{L}}s},
  longplural={Cumulative Energy Losses}
]{cel}{Ce\ensuremath{\mathcal{L}}}{Cumulative Energy Loss}
\newacronym{mss}{MSS}{multi-scale spectral loss}
\newacronym{sot}{SOT}{spectral optimal transport}
\newacronym{tfw}{\ensuremath{\mathrm{TF}\mathcal{W}_2}}{time--frequency Wasserstein distance}
\newacronym{bicul}{\ensuremath{\mathrm{BiCu}\mathcal{L}}}{bidirectional cumulative-energy loss}
\newacronym{stft}{STFT}{short-time Fourier transform}
\newacronym{fft}{FFT}{fast Fourier transform}
\newacronym{dft}{DFT}{discrete Fourier transform}
\newacronym{fm}{FM}{frequency modulation}
\newacronym{ir}{IR}{impulse response}
\newacronym{mir}{MIR}{music information retrieval}
\newacronym{midi}{MIDI}{Musical Instrument Digital Interface}
\newacronym{rms}{RMS}{root-mean-square}
\newacronym{cdf}{CDF}{cumulative distribution function}
\newacronym{hrc}{HRC}{half-raised-cosine}
\newacronym{df2}{DF2}{direct form II}
\newacronym{dc}{DC}{direct current}
\newacronym{gpu}{GPU}{graphics processing unit}
\newacronym{mae}{MAE}{mean absolute error}
\newacronym{lsd}{LSD}{log-spectral distance}
\newacronym{logq}{Log-Weigh}{log-frequency quadrature}
\newacronym{hpc}{HPC}{high-performance computing}
\newacronym{jnd}{JND}{just-noticeable difference}
\newacronym{dwg}{DWG}{Digital Waveguide}


\begin{document}
\maketitle

\begin{abstract}
\acrlong{ddsp} enables interpretable, unsupervised recovery of sound processor parameters via analysis by synthesis. However, it relies on waveform and spectral pointwise losses that do not encode energy displacement, needed to obtain essential parameters like onset time and pitch. In this work, we propose \glspl{cel}, which compare \acrshort{stft} power accumulated in time and frequency directions. We use them to recover the onset and pitch of sparse plucks rendered by a differentiable digital waveguide whose delay length defines pitch. Across 84,480 candidate--target comparisons containing one to eight events, \glspl{cel} give the strongest phrase-wide gradient alignment. We then compare the nine objectives reaching at least .10 single-event alignment across 6,750 musical-phrase fits. We believe these losses will further support unsupervised optimisation of onset timing, pitch, and delay-line length, and provide a PyTorch library for future applications\footnote{\textcolor{red}{link to library should be here}}.
\end{abstract}

\begin{IEEEkeywords}
differentiable digital signal processing, digital waveguide, audio loss, analysis by synthesis, event parameter estimation
\end{IEEEkeywords}

% Queue the cumulative illustration on page 1 so it occupies page 2.
\begin{figure*}[!t]
\centering
\includegraphics[width=\textwidth]{\PaperRoot/figures/cumulative_surfaces.pdf}
\caption{STFT power of a constant amplitude logarithmic 20-to-1000~Hz sine sweep over two seconds, and its resulting cumulative surfaces for the direction pairs $(d_t,d_f)$.
The shared linear colour scale runs from 0 to 1, with power normalised by peak
power for the spectrogram and total target power ($S_q/m$) for the
cumulative-power surfaces. Logarithmic frequency axes are used for display
only; accumulation uses linear power on the original STFT grid. Arrows mark
starting corners and accumulation directions.}
\label{fig:cumulative-surfaces}
\end{figure*}

\section{Introduction}
\glsresetall
Engel et al. coined the term \Gls{ddsp}, as the process of embedding audio processors in automatic differentiation graphs for efficient and interpretable unsupervised analysis by synthesis~\cite{engel2020ddsp}. Since then, it has been widely adopted in multitude of applications, such as neural waveshaping, \gls{fm} synthesis, reverberation, vocal tract modelling and effects modulation ~\cite{hayes2021waveshaping, caspe2022ddx7, dalsanto2023diffdn, sudholt2023vocal, yu2024torchlpc, mitcheltree2025modulation, carson2026gradient}; for an early comprehensive review, we redirect the reader to Hayes et al.~\cite{hayes2024review}.

In \gls{ddsp}, controls are mostly predicted as either a constant set of parameters, or dense frame trajectories, naturally fit for \gls{ir} prediction or continuously driven sounds respectively. However, that is not how many instruments are instinctively performed. Previously, we proposed an event-set predictor for the recovery of sparse guitar plucks \cite{tablasdepaula2026karplus}, forming note events with onset, pitch ($f_0$) and velocity among other physical parameters. This work expands this framework with the purpose of joining common \gls{mir} representations, where plucked string is represented as \gls{midi}, sheet music or guitar tablatures ~\cite{riley2024gaps, loth2025goat,loth2025guitarflow} with \gls{ddsp}.

However, $f_0$ and onset time, representing a displacement of energy, are notoriously difficult to predict. This is due to the fact that~\gls{ddsp}     relies on waveform and pointwise spectral losses, such as \gls{mss}, prompt to oscillatory local minima and useless when there is no energy overlap \cite{wang2019nsf, turian2020loss}. Recent revisions of \gls{mss} aimed to reduce the former issue by reducing spectral leaking \cite{schwar2023mss}, but do not address the latter. Complex surrogates were proposed but remain synthesiser-dependant \cite{hayes2023sinusoidal}. The literature commonly eludes this problematic via external detectors or synthetic labels, the first inherit the detectors' errors and veto further end-to-end training of $f_0$ \cite{engel2020inverse, engel2020ddsp, caspe2022ddx7, sudholt2023vocal}, while the latter generalises poorly to out of domain data \cite{tablasdepaula2026karplus}.

Recent years have seen the adoption of \gls{ot}, which measures the shortest energy displacement cost, in machine learning \cite{monge1781deblais,cuturi2013sinkhorn,peyre2019computational} and audio applications, such as music transcription and source separation \cite{flamary2016optimal, rolet2018blind}. Torres et al. were the first to apply it to \gls{ddsp} for harmonic parameter estimation, as a frame-wise spectral $\mathcal{W}_2$, \gls{sot} \cite{torres2024sot}; followingly, Fabiani et al. proposed a sliced formulation of a joint time--frequency \gls{ot} as a similarity metric, the \gls{tfw} ~\cite{fabiani2024jtfot}.

While promising, we showed that \gls{sot} is unreliable for joint onset--$f_0$ optimisation, and proposed \gls{tfw} as future work \cite{tablasdepaula2026karplus}. Hence, we propose recovering musical phrases via optimisation of $f_0$ and onset time of plucks rendered by a simple digital waveguide \cite{smith2010physical}, comparing \gls{tfw} with a newly proposed family of losses, \glspl{cel}, which compare \gls{stft} power accumulation in time and frequency bins.

%OT traces to Monge's earth-moving problem~\cite{monge1781deblais} and measures
%displacement using support geometry~\cite{peyre2019computational}.
%Scalable entropic regularisation~\cite{cuturi2013sinkhorn} has supported audio
%applications including spectral unmixing for music
%transcription~\cite{flamary2016optimal} and frequency-aware NMF for source
%separation~\cite{rolet2018blind}. Torres et al. brought OT into \gls{ddsp} as a
%frame-wise spectral $\mathcal{W}_2$ objective for joint $f_0$ and harmonic
%amplitudes, augmented by a pointwise multi-scale term and cumulative-mass
%cutoff~\cite{torres2024sot}; Fabiani et al. subsequently used sliced projections
%for joint time--frequency displacement~\cite{fabiani2024jtfot}.

%We retain their event representation but
%optimise phrases rendered by a simple digital
%waveguide~\cite{smith2010physical,depaula2026waveguides}, comparing joint
%time--frequency OT with \gls{bicul}, which compares normalised STFT-power
%accumulations towards all four corners. The renderer fixes Fourier-shifted
%excitation and Thiran fractional-delay propagation, with event-wise string-state
%reset for phrase recovery.

Hence, our contributions are twofold:
\begin{itemize}
    \item A family of losses, \glspl{cel}, that can recover onset time and $f_0$ in the single-pluck landscape.
    \item An unsupervised method for recovering monophonic plucked string musical phrases that is superior to previously proposed loss configurations.
\end{itemize}



\section{Cumulative Energy Losses}
\label{sec:losses}

We propose a new family of losses, \Glspl{cel}, that compare accumulated
spectral power, exposing displacement through changes between the original
and shifted energy. Let $P_{k,n}$ and $\hat P_{k,n}$ denote target and candidate
\gls{stft} power at frequency bin $k$ and frame $n$, using a periodic Hann
window, a 256-point \gls{fft}, and a 64-sample hop.

For a chosen accumulation direction in time, $d_t$, we read frames from
first to last ($\rightarrow$) or last to first ($\leftarrow$). Similarly,
the frequency direction $d_f$ reads bins from low to high ($\uparrow$)
or high to low ($\downarrow$). A direction pair $q=(d_t,d_f)$ combines
one choice for each axis, giving four possible pairs.
For each pair, the cumulative-sum operator $C^t_{d_t}$ replaces each bin's
power by the sum of that power and all preceding frames at the same frequency
in the chosen time direction. The operator $C^f_{d_f}$ performs the corresponding cumulative sum
along frequency. Applying both operators gives the target and candidate
cumulative-power surfaces, $S_q$ and $\hat S_q$:
\begin{equation*}
S_q=C^f_{d_f}(C^t_{d_t}(P)),\qquad
\hat S_q=C^f_{d_f}(C^t_{d_t}(\hat P)).
\end{equation*}
Each surface is a two-dimensional image used to compute the loss. Its value
at $(k,n)$ contains the power in the rectangle from the chosen starting
corner to that bin inclusive, as shown in Fig.~\ref{fig:cumulative-surfaces}.



To compare these images, we divide both by the nonzero total target power
$m=\sum_{k,n}P_{k,n}=\max S_q$ and apply a pointwise feature transformation
$\phi$. For each direction pair, let $E_q(k,n)$ be the candidate-minus-target
feature error and $\ell_q$ its \gls{rms} over all bins and frames. For a selected
set of direction pairs $\mathcal Q$, the scalar loss $\mathcal{L}_{\mathcal Q}$
averages these directional errors\footnote{Although a full mathematical
 equivalence is outside the scope of this work, we note that for 1D unit-mass
 distributions on a uniform grid, our \Glspl{cel} with a single accumulation direction
 and $\phi(u)=u$ yield the Cram\'er distance up to fixed grid and RMS
 normalisation. In turn, this relates \Glspl{cel} to the $1$-Wasserstein distance, which uses $L_1$ on Cumulative Distribution Functions, instead of Cram\'er's L2 \cite{szekely2013energy}\cite[Remark~2.30]{peyre2019computational}.};
$|\mathcal Q|$ is the number of pairs:
\begin{equation*}
\begin{aligned}
E_q(k,n)&=\phi(\hat S_q(k,n)/m)-\phi(S_q(k,n)/m),\\
\mathcal{L}_{\mathcal Q}&=\frac{1}{|\mathcal Q|}\sum_{q\in\mathcal Q}\ell_q,
\qquad\ell_q=\operatorname{RMS}(E_q).
\end{aligned}
\end{equation*}


\section{Differentiable Plucked-String Musical Phrases}
\label{sec:renderer}
We use \Glspl{dwg} as a common and efficient choice for modelling plucked strings \cite{smith2010physical,depaula2026waveguides}. Our model has two oppositely travelling rails, each split at the excitation point into nut-side and bridge-side delays of $D_1$ and $D_2$ samples, respectively, as depicted in Fig.~\ref{fig:waveguide}. For ideal delays and phase-inverting terminations, $f_0=f_s/[2(D_1+D_2)]$, where $f_s$ is the sample rate; the implemented model also compensates for interpolation and loss-filter phase. The relative pluck position arises as $D_2/(D_1+D_2)=0.23$ from the bridge. We synthesise two-second signals at $f_s=4$~kHz.

\begin{figure}[!b]
\centering
\includegraphics[width=\columnwidth]{\PaperRoot/figures/Waveguide.pdf}
\caption{Two-rail digital waveguide for plucked-string synthesis, with
nut-side delay $D_1$ and bridge-side delay $D_2$ on each rail.}
\label{fig:waveguide}
\end{figure}

% Queue the single-column slices for page 3.
\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{\PaperRoot/figures/loss_sweeps.pdf}
\caption{Single-pluck loss slices around a 160-Hz, 1.0-s target. Top: time
shift with pitch fixed; bottom: frequency shift with onset fixed. Each curve is
independently min--max normalised over 3201 samples.}
\label{fig:loss-sweeps}
\end{figure}

Nut and bridge reflection gains are $-1$ and $-H_L(z)$, respectively, where
$H_L$ is the loss-filter transfer function, $z$ the transform variable, and
$z^{-1}$ a one-sample delay:
\begin{equation*}
H_L(z)=g\frac{1-a_1}{1-a_1z^{-1}},\qquad g=0.99,\quad a_1=0.2.
\end{equation*}
Here $g$ controls the DC gain and increasing $a_1$ increases high-frequency
attenuation, providing decay and damping controls. The resulting decay times
depend on $f_0$, since the loss filter is traversed once per round trip,
as also illustrated for a related model in
\cite[Fig.~3]{tablasdepaula2026karplus}. Output is the bridge-incident
travelling velocity.

Each new event excites a \gls{hrc},
\begin{equation*}
e_0(t)=\begin{cases}
\dfrac{A_e}{2}[1-\cos(\pi t/T_e)],&0\leq t<T_e,\\
0,&\text{otherwise,}
\end{cases}
\end{equation*}
where $e_0(t)$ is           amplitude at elapsed time $t$, $A_e=0.8$ its amplitude scale, and $T_e=10$~ms its duration. As a differentiable onset placement method, we use a Fourier phase-ramp delay for, using FLAMO~\cite{dalsanto2025flamo}. To place an onset by $\delta_o$ samples, we multiply the spectrum of $e_0$ by $e^{-j\omega\delta_o}$, where $\omega$ is angular frequency in radians per sample and $j^2=-1$. The excitation is zero-padded to an FFT length $2.048$ times the full signal length to reduce circular wrap-around~\cite[Ch.~7]{smith2007dft}. We constrain the DC and Nyquist bins to be real to preserve Hermitian symmetry~\cite[Ch.~7]{smith2007dft}. Shifted event spectra are summed before one inverse FFT, retaining the first two seconds as the summed excitation.

For gradient-based $f_0$ optimisation, we use real-valued delays with Thiran
all-pass interpolation.
These filters have unity magnitude and maximally flat group delay at
DC~\cite[p.~49]{laakso1996splitting}.
Each path combines an integer delay with an all-pass section. For an order-$M$
section, the approximated delay must exceed $M-1$ samples for
stability~\cite{laakso1996splitting}.
We use orders 1 and 3 on the bridge-side and nut-side paths, respectively:
at the upper end of our 80--320~Hz pitch range, the shorter path requires
less than two samples, precluding a stable order-3 section. We iteratively
adjust the delays to account for the all-pass and loss-filter phase at $f_0$.

Finally, we collapse the waveguide algebraically into a rational transfer
function and evaluate it in \gls{df2} as required for PhilTorch with its TorchLPC
backend for fast GPU backpropagation~\cite{yu2025philtorch,yu2024torchlpc}. For phrase synthesis, we reset
the recurrence history at each incoming event's onset, rounded down to a
sample boundary and detached from differentiation. Resetting smooths the
multi-pluck loss landscapes in our diagnostic; we hypothesise that removing
interference with residual string vibration contributes to this effect\footnote{Supplementary
loss landscapes at
\url{https://ptablasdpaula.github.io/ICASSP27-Phrase} document onset placement, delay interpolation choices and the state-reset
diagnostic and comparison of \gls{df2} and literal \gls{dwg} implementations.}.

% Queue the double-column result while page 3 is still being composed so that
% IEEEtran can place it at the top of page 4.
\begin{table*}[!t]
\centering
\caption{\normalfont\textcolor{blue}{Median Hungarian-assigned pitch and onset
errors over 150 target phrases per event count; lower is better.}}
\label{tab:pilot-compact}
\input{\PaperRoot/figures/median_recovery_table_pending.tex}
\end{table*}

\section{Experiments}
\label{sec:experiments}

\textbf{Synthetic data.}
All experiments use the two-second, 4-kHz synthesis configuration in
Sec.~\ref{sec:renderer}. An $N$-event phrase contains pitches
$p=\log_2(f_0/80)\in[0,2]$~octaves and onsets $t\in[0.2,1.8]$~s, with
$N\in\{1,2,4,6,8\}$. We use scrambled Latin hypercube sampling (LHS) to
stratify the sampled coordinates~\cite{mckay1979comparison}; target onsets are
separated by at least 50~ms, while candidate coordinates span the complete
registered domain.

\textbf{Metrics.}
For an unordered prediction, the Hungarian method~\cite{kuhn1955hungarian}
selects the event permutation $\pi$ minimising
$\sum_i[(\hat p_i-p_{\pi(i)})^2+(\hat t_i-t_{\pi(i)})^2]$, so one octave and
one second have equal assignment cost. This assignment supplies the
within-phrase pitch and onset mean absolute errors, reported in cents and
milliseconds, respectively. It also reorders the target for our phrase-wide
gradient cosine: with $\boldsymbol\theta=(p_1,t_1,\ldots,p_N,t_N)$ and descent
$\mathbf d=-\nabla_{\boldsymbol\theta}\mathcal L$, we measure the cosine
between $\mathbf d$ and
$\boldsymbol\theta^\star_\pi-\boldsymbol\theta$. This scale-independent
alignment follows the use of gradient cosine similarity to identify helpful
directions~\cite{du2018gradient}; $1$, $0$, and $-1$ indicate descent directly
towards, orthogonal to, and away from the assigned target. We report its mean
and sample standard deviation across candidate phrases. As an
objective-independent audio metric, \gls{lsd} is the \gls{rms} difference
between periodic-Hann \acrshort{fft}-256/hop-64 log-magnitude spectra, using a
common target-relative $-100$-dB magnitude floor.

\textbf{Baselines.}
We retain the cited losses and state only their adaptations to our 4-kHz
inputs. Alongside waveform $L_1$ and $L_2$, SS uses periodic-Hann
\acrshort{fft}-256/hop-64 magnitudes. LinMSS uses Hann-windowed FFT sizes 16,
32, 64, 128, 256, and 512 with quarter-length hops~\cite{wang2019nsf,engel2020ddsp}.
SmoMSS uses the time-scaled flat-top window/hop pairs 17/8, 31/15, 67/33,
127/63, 257/128, and 509/254~\cite{schwar2023mss}. \Gls{sot} uses flat-top
\acrshort{fft}-512/hop-64 transport and its $0.05$-weighted LinMSS
component~\cite{torres2024sot}. Both \gls{tfw} variants use globally normalised
periodic-Hann \acrshort{fft}-256/hop-128 magnitudes and ten projections: the
published coordinates set $1$~s${}=1000$~Hz, while our log-frequency adaptation
sets $1$~s${}=1$~octave through
$\log_2[\max(f,20)/20]$~\cite{fabiani2024jtfot}.

\textbf{CeL variants.}
The base Ce$\mathcal L$ averages all four diagonal direction pairs, uses
$\phi(u)=\sqrt{u}$, and applies a uniform \gls{rms}. logCe$\mathcal L$ instead
uses Log-Weighing: octave and time-bin widths weight the final \gls{rms}, so
equal octave spans receive equal total frequency weight while the cumulative
surfaces remain unchanged. decCe$\mathcal L$ retains uniform \gls{rms} but
makes accumulation local by multiplying contributions at separations
$\Delta t$ and $\Delta f$ by
$w(\Delta t;1\,\mathrm{s})w(\Delta f;1000\,\mathrm{Hz})$, where
$w(d;H)=\max\{0,1-\log(1+9d/H)/\log 10\}$. Finally, tlogCe$\mathcal L$ applies
Log-Weighing to only the two forward-time pairs
$(\rightarrow,\uparrow)$ and $(\rightarrow,\downarrow)$.

\subsection{Gradient Analysis}
\label{sec:gradient-diagnostic}
We ask how closely each loss's descent direction aligns with the displacement
that recovers a target phrase, before taking an optimiser step. The one-event
target is fixed at 160~Hz and 1.0~s. For each larger cardinality, 32 target
phrases sample the registered ranges with 50-ms onset separation. A scrambled
joint LHS samples all $2N$ candidate coordinates. The \emph{Joint} condition
retains both perturbations; \emph{Known time} restores target onsets and varies
only pitch; \emph{Known $f_0$} restores target pitches and varies only onset.
Joint is tested at $N\in\{1,2,4,6,8\}$ and both conditional cases at
$N\in\{1,2,4\}$.

Each condition begins with 256 candidates per target and doubles to 512 or
1024 where an independent repeat changes any loss's componentwise success rate
by more than two percentage points. This gives 84,480 paired candidate--target
comparisons, shared across the eight baselines and four Ce$\mathcal L$
variants above. Hungarian matching remains unrestricted in the conditional
cases, and all gradient components are retained, so movement in a known
coordinate reduces alignment.

\begin{figure}[t]
\centering
\includegraphics[width=0.88\columnwidth]{\PaperRoot/figures/gradient_alignment.pdf}
\caption{Phrase-wide gradient cosine mean $\pm$ sample standard deviation;
higher is better. Columns give event count; colours span $[-1,1]$.}
\label{fig:gradient-alignment}
\end{figure}

\subsection{Musical Phrase Recovery}
Seed 2027 generates 150 scrambled-LHS targets at each cardinality. Every
candidate begins with all pitches at 160~Hz and onsets at equal-cell centres
over 0.2--1.8~s; independent logits constrain both coordinates to their
registered ranges. We retain the nine objectives whose one-event Joint mean
cosine rounds to at least $.10$: SS, LinMSS, SmoMSS, \gls{tfw},
log-\gls{tfw}, Ce$\mathcal L$, logCe$\mathcal L$, decCe$\mathcal L$, and
tlogCe$\mathcal L$.

Each of the resulting 6,750 fits uses Adam with learning rate $.05$, default
moments and epsilon, and zero weight decay. Division by each phrase's initial
loss conditions the step scale while leaving reported losses raw. An
independent ReduceLROnPlateau scheduler uses factor $.5$, patience 200, relative
improvement threshold $10^{-4}$, and minimum learning rate $10^{-5}$.
Early stopping uses its own 1000-update patience, with a 20,000-update cap. We
report the final iterate without rollback or optimiser-state reset.

\begin{figure}[t]
\centering
\setlength{\abovecaptionskip}{0pt}
\includegraphics[width=0.74\columnwidth]{\PaperRoot/figures/lsd_violin.pdf}
\vspace{-5.0mm}
\caption{\textcolor{blue}{Per-phrase \acrshort{lsd} distributions over the same
targets; lower is better.}}
\label{fig:lsd}
\end{figure}

\section{Conclusion}
We introduced \glspl{cel} for joint pluck-onset and pitch recovery. Across
84,480 comparisons, they give the highest mean Joint gradient cosine at every
cardinality and retain $.10$--$.14$ at eight events, against at most $.04$ for
the baselines. Their advantage also holds when only pitch or onset is unknown.
\textcolor{blue}{The 6,750 matched fits will determine the best objective for
pitch, onset, and spectral fidelity.}

\clearpage
\section*{Acknowledgment}
Pablo Tablas de Paula is supported by the UKRI Centre for Doctoral Training in
Artificial Intelligence and Music (grant EP/S022694/1). This research utilised
Queen Mary's Apocrita \gls{hpc} facility, supported by QMUL Research-IT
(\url{https://doi.org/10.5281/zenodo.438045}), using NVIDIA V100 and A100 \glspl{gpu}.

\bibliographystyle{IEEEtran}
\typeout{ICASSP bibliography database: \PaperRoot/references.bib}
\bibliography{\PaperRoot/references}
\end{document}
