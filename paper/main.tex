\documentclass[conference]{IEEEtran}
% Overleaf runs BibTeX from the project top level even when the selected main
% document lives in paper/.  Resolve one prefix for both a full-repository
% Overleaf project and a standalone upload of the paper/ directory.
\ifdefined\PaperRoot\else
  \IfFileExists{paper/references.bib}{%
    \newcommand{\PaperRoot}{paper}%
  }{%
    \newcommand{\PaperRoot}{.}%
  }%
\fi
% IEEE conference template version as of 6/27/2024.

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{textcomp}
\usepackage{url}
\usepackage{xcolor}
% Keep IEEE's typography and page geometry while avoiding oversized gaps
% around floats.
\setlength{\textfloatsep}{7pt plus 2pt minus 2pt}
\setlength{\floatsep}{6pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{7pt plus 2pt minus 2pt}
\setlength{\dblfloatsep}{6pt plus 2pt minus 2pt}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\title{Gradient Descent Optimization of Plucked-String Musical Phrases via
Cumulative Energy Loss}
\author{\IEEEauthorblockN{Pablo Tablas de Paula\IEEEauthorrefmark{1},
Sebastian J. Schlecht\IEEEauthorrefmark{2},
Emmanouil Benetos\IEEEauthorrefmark{1}, and
Joshua D. Reiss\IEEEauthorrefmark{1}}
\IEEEauthorblockA{\IEEEauthorrefmark{1}Centre for Digital Music,
Queen Mary University of London, London, UK}
\IEEEauthorblockA{\IEEEauthorrefmark{2}Multimedia Communications and Signal Processing,\\
Friedrich-Alexander-Universit\"at Erlangen-N\"urnberg, Erlangen, Germany}
\IEEEauthorblockA{\texttt{p.tablasdepaula@qmul.ac.uk};
\texttt{sebastian.schlecht@fau.de}}}

% LaTeX control words cannot contain underscores, so the requested
% \l_one and \l_two source names are represented by \lone and \ltwo.
\newcommand{\lone}{\ensuremath{\mathcal{L}_1}}
\newcommand{\ltwo}{\ensuremath{\mathcal{L}_2}}
\newcommand{\ddsp}{DDSP}
\newcommand{\mss}{MSS}
\newcommand{\sot}{SOT}
\newcommand{\tfw}{\ensuremath{\mathrm{TF}\mathcal{W}_2}}
\newcommand{\bicul}{\ensuremath{\mathrm{BiCu}\mathcal{L}}}


\begin{document}
\maketitle

\begin{abstract}
Differentiable digital signal processing enables interpretable, unsupervised
analysis by synthesis, but onset and fundamental frequency remain difficult to
optimise because waveform and pointwise spectral losses do not encode
displacement direction. External detectors avoid this geometry but prevent the
loss from correcting their estimates, limiting reconstruction. We propose
\bicul{}, a cumulative-energy loss that compares STFT power
accumulated in both directions of time and frequency. We use it to recover the
onset and fundamental frequency of sparse plucks rendered by a differentiable
digital waveguide, whose delay length sets pitch. In a single-pluck landscape,
\bicul{} is the only objective with all 120 displayed descent directions
target-directed under the fixed Fourier--Thiran renderer. Across 6000 fits of
phrases containing one to eight plucks, \bicul{} gives more reliable joint
onset--pitch recovery than waveform, spectral, and optimal-transport objectives.
These results support unsupervised optimisation of event timing, pitch, and
delay-line length under the controlled renderer.
\end{abstract}

\begin{IEEEkeywords}
differentiable digital signal processing, digital waveguide, audio loss, analysis by synthesis, event parameter estimation
\end{IEEEkeywords}

\section{Introduction}
Differentiable digital signal processing (\ddsp) embeds audio processors in
automatic-differentiation graphs for interpretable analysis by
synthesis~\cite{engel2020ddsp}; Hayes et al. survey its use in music and speech
synthesis~\cite{hayes2024review}. Applications include efficient neural
instrument synthesis through learned waveshaping~\cite{hayes2021waveshaping}, structured
modulation discovery from synthesizer audio~\cite{mitcheltree2025modulation},
and direct feedback-delay-network optimisation to reduce reverberation
coloration~\cite{dalsanto2023diffdn}. Controls are nevertheless typically global
parameters or dense frame-rate trajectories, suited to static processors and
continuously driven sounds. Plucked and struck strings are instead naturally
organised as note events with onset, pitch, duration, and velocity, as in MIR
and symbolic music~\cite{hawthorne2018onsets}.

This event representation does not solve loss geometry. Pointwise spectral and
waveform losses can mislead when displaced energy does not
overlap~\cite{turian2020loss}; proposed complex surrogates remain
synthesizer-dependent~\cite{hayes2023sinusoidal}. External $f_0$ detectors or
synthetic parameter labels bypass this geometry but prevent the reconstruction
loss from correcting their estimates~\cite{engel2020ddsp,engel2020inverse}.
Optimal transport (OT) instead measures displacement using support
geometry~\cite{peyre2019computational}.
Scalable entropic regularisation~\cite{cuturi2013sinkhorn} has supported audio
applications including spectral unmixing for music
transcription~\cite{flamary2016optimal} and frequency-aware NMF for source
separation~\cite{rolet2018blind}. Torres et al. brought OT into \ddsp{} as a
frame-wise spectral $\mathcal{W}_2$ objective for joint $f_0$ and harmonic
amplitudes, augmented by a pointwise multi-scale term and cumulative-mass
cutoff~\cite{torres2024sot}; Fabiani et al. subsequently used sliced projections
for joint time--frequency displacement~\cite{fabiani2024jtfot}.

Tablas de Paula et al. used an event-based plucked-string model to jointly
estimate onset, $f_0$, and timbre, but synthetic supervision transferred poorly
to real audio and spectral OT was unreliable for joint onset--pitch
optimisation, leading them back to external
detectors~\cite{tablasdepaula2026karplus}. We retain their event representation but
optimise phrases rendered by a simple digital
waveguide~\cite{smith2010physical,depaula2026waveguides}, comparing joint
time--frequency OT with \bicul{}, which compares normalised STFT-power
accumulations towards all four corners. The renderer fixes Fourier-shifted
excitation and Thiran fractional-delay propagation, with event-wise string-state
reset for phrase recovery.

Hence our contributions are threefold:
\begin{itemize}
    \item We formulate \bicul{} for direct onset--pitch recovery and provide a PyTorch implementation.
    \item We compare six objectives over the full registered onset--pitch range for one centred pluck; under the fixed renderer, \bicul{} is the only one whose 120 displayed descent directions all point towards the target.
    \item We benchmark eight loss configurations across 6000 phrase fits and use a three-event diagnostic to show that state reset smooths the plotted multi-pluck DF2 sweeps.
\end{itemize}

\section{Bidirectional cumulative-energy loss}
\label{sec:losses}
The bidirectional cumulative-energy loss, \bicul{}, compares STFT power
accumulated towards all four time--frequency corners. Displacement alters values
between the energy and each boundary, exposing early/late and low/high direction
without favouring a boundary. Let $P_{k,n}$ and $\hat P_{k,n}$ denote target and
candidate periodic-Hann FFT-256/hop-64 power at frequency bin $k$ and frame $n$.
For $q=(d_t,d_f)$, $d_t\in\{\rightarrow,\leftarrow\}$ and
$d_f\in\{\uparrow,\downarrow\}$ select accumulation direction; $C^t_{d_t}$ and
$C^f_{d_f}$ are the corresponding cumulative sums:
\begin{equation*}
\begin{aligned}
S_q&=C^f_{d_f}(C^t_{d_t}(P)),\\
\hat S_q&=C^f_{d_f}(C^t_{d_t}(\hat P)),\\
m_q&=\max S_q,\qquad R_q=\sqrt{\max(S_q/m_q,10^{-12})},\\
\hat R_q&=\sqrt{\max(\hat S_q/m_q,10^{-12})},\\
\ell_q&=\operatorname{RMS}(R_q-\hat R_q).
\end{aligned}
\end{equation*}
Here $S_q,\hat S_q$ are the target/candidate cumulative surfaces; $m_q$ is the
target maximum; $R_q,\hat R_q$ are target-normalised square roots floored at
$10^{-12}$; and $\ell_q$ is their root-mean-square (RMS) difference over
$(k,n)$. \bicul{} averages all four $\ell_q$ without event coordinates,
detectors, or salience weights.

\bicul{} is related to CDF-based Cram\'er distances: in one dimension, the
Cram\'er discrepancy integrates squared CDF
differences~\cite{szekely2013energy}. With equal-mass unit-normalised power, one
accumulation direction, and no square-root feature, $\ell_q$ becomes a discrete
axis-aligned two-dimensional Cram\'er-type discrepancy. Both sense displacement
without pointwise overlap because a shift changes the intervening cumulative
region. \bicul{} is not the standard distance: target-only normalisation admits
unequal candidate energy, square roots compress dominant regions, and four
orientations avoid privileging one boundary.

Phrase recovery additionally evaluates Log-Q \bicul. It retains the same STFT,
cumulative surfaces, target-maximum normalisation, and square-root features, but
replaces the final uniform-bin RMS. For
$E_q(k,n)=\hat R_q(k,n)-R_q(k,n)$, the candidate-minus-target surface error,
the directional loss becomes
\begin{equation*}
\ell^{\rm quad}_{q}=\left[\frac{\sum_{k,n}
w^f_{d_f,k}w^t_{d_t,n}E_q(k,n)^2}
{\sum_{k,n}w^f_{d_f,k}w^t_{d_t,n}}\right]^{1/2}.
\end{equation*}
Here $w^f_{d_f,k}$ and $w^t_{d_t,n}$ are direction-aligned adjacent-bin widths
in octaves and seconds, respectively; the frequency coordinate is
$\log_2[\max(f,20)/20]$, whose 20-Hz floor gives a finite origin. Log-Q denotes
log-frequency quadrature, not a constant-Q transform. Both sums span $(k,n)$,
$\ell^{\rm quad}_q$ replaces $\ell_q$, and all other operations are unchanged.

\begin{figure}[!t]
\centering
\setlength{\abovecaptionskip}{0pt}
\includegraphics[width=\columnwidth]{\PaperRoot/figures/loss_landscapes.pdf}
\vspace{-5.8mm}
\caption{Fourier--Thiran single-pluck loss landscapes. The cross marks the
target, white arrows show unit descent, and each white upper-right number is the
percentage of 120 non-target directions pointing towards the target.}
\label{fig:landscapes}
\end{figure}

\section{Differentiable phrase renderer}
\label{sec:renderer}
The phrase renderer is the reciprocal two-rail digital waveguide in
Fig.~\ref{fig:waveguide}. Each pluck launches oppositely travelling waves into
nut- and bridge-side paths. Their delay-length sum sets $f_0$, while their ratio
sets pluck position~\cite{depaula2026waveguides}, fixed at 0.23 from the bridge.

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{\PaperRoot/figures/Waveguide.pdf}
\caption{Two-rail digital waveguide renderer. $D_1$ and $D_2$ are the one-way
nut- and bridge-side delays; blue and red denote right- and left-travelling
waves. Ignoring loop-filter phase, the round-trip delay is $2(D_1+D_2)$ samples
and $f_0=f_s/[2(D_1+D_2)]$; the implementation phase-corrects the segment
delays for $H_L(z)$.}
\label{fig:waveguide}
\end{figure}

Nut and bridge reflection gains are $-1$ and $-H_L(z)$, respectively, where
$H_L$ is the loss-filter transfer, $z$ the transform variable, and $z^{-1}$ a
one-sample delay:
\begin{equation*}
H_L(z)=g\frac{1-a_1}{1-a_1z^{-1}},\qquad g=0.99,\quad a_1=0.2.
\end{equation*}
$g$ is low-frequency round-trip gain and $a_1$ the one-pole coefficient
increasing high-frequency attenuation. They produce a complex, $f_0$-dependent
RT60~\cite[Fig.~3]{tablasdepaula2026karplus}. Output is bridge-incident
travelling velocity. All experiments use the fixed configuration described
below for both target and candidate.

Audio is float64, 2~s long, and sampled at 4~kHz. Each event excites a zero-onset
half-raised-cosine (HRC)
\begin{equation*}
e_0(t)=\begin{cases}
\dfrac{A_e}{2}[1-\cos(\pi t/T_e)],&0\leq t<T_e,\\
0,&\text{otherwise,}
\end{cases}
\end{equation*}
where $e_0(t)$ is amplitude at elapsed time $t$, $A_e=0.8$ its peak, and
$T_e=10$~ms its duration. To place an onset by $\delta_o$ samples, we multiply
the 262,144-point spectrum of $e_0$ by FLAMO's phase response
$e^{-j\omega\delta_o}$~\cite{dalsanto2025flamo}, where $\omega$ is angular
frequency in radians per sample and $j^2=-1$. Event spectra are accumulated,
the DC and Nyquist bins are constrained to be real so the implied negative
frequencies preserve Hermitian symmetry, and one inverse FFT produces the
summed excitation; its first 8000 samples form the rendered interval.

Continuous $f_0$ likewise requires real-valued rail delays: integer-only lines
quantise pitch and provide no useful $f_0$ gradient. We split the phase-corrected
one-way string delay at the fixed pluck position into a long nut-side path and a
short bridge-side path, then approximate them with maximally-flat Thiran
all-pass delays~\cite{laakso1996splitting}. Their respective orders are 3 and 1.
A causal, stable order-$M$ Thiran section requires delay greater than $M-1$;
over 80--320~Hz, the shortest bridge-side delay falls below the order-3 limit,
whereas order 1 remains valid. Iterative rail tuning accounts for all-pass and
loop-filter phase. Thus all reported experiments fix Fourier onset placement
and Thiran 3/1 propagation.\footnote{An auxiliary \bicul-only screen also covers
analytic, Lagrange-5, Fourier, and Thiran-1 onset placement and Linear,
Lagrange, Fourier, and Thiran propagation:
\url{https://ptablasdpaula.github.io/ICASSP27-Phrase/#renderer-screen}. It is
a sensitivity diagnostic, not the basis for the fixed renderer or an
objective-independent ranking.}

The Thiran rails, reflections, and termination are algebraically collapsed into
one event-conditioned rational transfer and evaluated in direct form II (DF2)
through PhilTorch and its TorchLPC backend~\cite{yu2025philtorch,yu2024torchlpc}.
At each detached event boundary $\lfloor t_i f_s\rfloor$, event-wise reset zeros
the recurrence history before the new excitation; the persistent diagnostic
instead carries it across events. All recursive campaign rendering runs on
NVIDIA GPUs.

\begin{figure}[!t]
\centering
\setlength{\abovecaptionskip}{0pt}
\includegraphics[width=\columnwidth,trim=0 20bp 194bp 0,clip]{\PaperRoot/figures/state_realization.pdf}
\vspace{-3.5mm}
\caption{Reset-DF2 target spectrogram and middle-event \bicul{} sweeps for
the three-pluck target $(110~\mathrm{Hz},0.5~\mathrm{s})$,
$(160~\mathrm{Hz},1.0~\mathrm{s})$, and $(280~\mathrm{Hz},1.5~\mathrm{s})$.
Purple solid curves retain state, yellow dashed curves reset it, red dashed
lines mark the middle event, and cyan dashed lines mark the other events. Each
curve is independently min--max normalised.}
\label{fig:state-realization}
\end{figure}

As shown in Fig.~\ref{fig:state-realization}, resetting the string state is
necessary to smooth the plotted DF2 loss sweeps in this three-event diagnostic;
Experiment II therefore uses event-wise reset.\footnote{The full DF2/Literal
comparison is available at
\url{https://ptablasdpaula.github.io/ICASSP27-Phrase/#state-realisation}.
Their reset curves appear coincident in this diagnostic only; generalisation is
outside the present scope.}

\section{Experiments and Methodology}
\subsection{Loss Objectives}
\label{sec:loss-objectives}
We compare six base objectives. \lone{} is mean absolute and \ltwo{} mean
squared sample error. Wang et al. introduced multi-resolution spectral
comparison~\cite{wang2019nsf}; Engel et al. adopted it as \mss{} in
\ddsp{}~\cite{engel2020ddsp}, and Schw\"ar and M\"uller later revisited its
design~\cite{schwar2023mss}. Following the latter, \mss{} sums squared \ltwo{}
distances between
$\log(1+|\operatorname{STFT}|)$ magnitudes, where $|\cdot|$ is magnitude and
$\log$ the natural logarithm. Its prime-length flat-top windows
$\{17,31,67,127,257,509\}$ and hops $\{8,15,33,63,128,254\}$ limit leakage
without aligning DFT bins. Torres et al.'s \sot{}~\cite{torres2024sot}
combines frame-normalised FFT-512/hop-64 flat-top STFT-power transport with
their unbalanced quantile cutoff and $0.05$ Hann-windowed linear-magnitude MSS.
Fabiani et al.'s \tfw{}~\cite{fabiani2024jtfot} globally normalises a
periodic-Hann FFT-256/hop-128 magnitude STFT and projects it along ten angles.
We retain their scaling $\widetilde t=(10^3\,\mathrm{Hz/s})t$, making 1~s
commensurate with 1000~Hz in the ground cost. Immediately after this canonical
configuration, Experiment II tests a $1\,\mathrm{s}=1\,\mathrm{oct}$ variant
that changes \tfw{} coordinates to
$\log_2[\max(f,20)/20]$ octaves and seconds; $f$ is bin-centre Hz and the
20-Hz floor gives a finite origin. The sixth base objective is
Sec.~\ref{sec:losses}'s \bicul; Experiment II additionally evaluates its Log-Q
variant defined there. Experiment I compares the six base objectives, while
Experiment II compares all eight configurations.

\begin{table*}[!t]
\centering
\caption{\normalfont Median Hungarian-matched pitch and onset errors over 150
phrases per event count; lower is better. Bold, underlined, and italic values
indicate first, second, and third place, respectively.}
\label{tab:pilot-compact}
\input{\PaperRoot/figures/median_recovery_table.tex}
\end{table*}

\subsection{Experiment I: Single-event Gradient Diagnostic}
\label{sec:gradient-diagnostic}
We use a centred 160-Hz, 1.0-s target and a target-inclusive 25-by-25 lattice.
Time is linear over $\pm0.8$~s and pitch logarithmic over $\pm1$ octave, so
this relative lattice spans the complete registered candidate ranges
0.2--1.8~s and 80--320~Hz. Surfaces are independently min--max normalised.
Unit descent directions $-\nabla\mathcal L$ are displayed on an 11-by-11
sublattice with the target omitted, leaving 120 directions. A direction is
target-directed when its dot product with the target displacement is positive
in the normalised onset--pitch coordinates.

Figure~\ref{fig:landscapes} shows the six loss surfaces and directions using
Sec.~\ref{sec:renderer}'s fixed Fourier--Thiran renderer. Target and candidate
share this configuration, isolating loss geometry from renderer mismatch. This
is one full-range diagnostic case, not evidence of target invariance or a
population estimate.

\subsection{Experiment II: Phrase Recovery}
Seed 2027 generates 150 scrambled-Latin-hypercube targets at each of one, two,
four, six, and eight events. Pitches are log-mapped to 80--320~Hz; onsets span
0.2--1.8~s with at least 50~ms separation.

All candidate pitches start at 160~Hz and onsets at equal-cell centres over
0.2--1.8~s; independent logits bound coordinates to those same onset and pitch
ranges. Adam uses learning rate 0.05, default moments/epsilon, and no weight
decay; initial-loss division equalises numerical scale but not reported values.
A relative raw-loss decrease of $10^{-4}$ resets patience. At patience 100 and
200, we restore the strict best, multiply learning rate by 0.3 (floor
$10^{-5}$), and clear optimiser state. We stop at patience 250 or 3000 updates,
retaining the best iterate.
With six base objectives plus two variants, the campaign comprises 150 targets
per event count $\times$ 8 losses $\times$ 5 event counts${}={}$6000 fits.

\begin{figure}[t]
\centering
\setlength{\abovecaptionskip}{0pt}
\includegraphics[width=0.88\columnwidth]{\PaperRoot/figures/lsd_violin.pdf}
\vspace{-5.0mm}
\caption{Per-phrase LSD distributions over the same targets; lower is better.}
\label{fig:lsd}
\end{figure}

Let $i$ index an unordered prediction and $j$ a target. Their assignment cost is
\begin{equation*}
d_{ij}=\left[\frac{\log_2(\hat f_{0,i}/f_{0,j})}{2}\right]^2+
\left[\frac{\hat t_i-t_j}{1.6}\right]^2.
\end{equation*}
Hats denote predictions; $\hat f_{0,i},f_{0,j}$ are pitches in Hz;
$\hat t_i,t_j$ are onsets in seconds; $2$ and $1.6$ are the full ranges in
octaves and seconds; and $d_{ij}$ is dimensionless. Hungarian assignment finds
the one-to-one pairing minimising $\sum d_{ij}$. Table~\ref{tab:pilot-compact}
reports its errors and Fig.~\ref{fig:lsd} the audio-error distributions.

We report across-phrase medians of within-phrase mean absolute pitch cents and
onset milliseconds. Periodic-Hann FFT-256/hop-64 log-spectral distance (LSD)
gives an objective-independent audio metric.

\section{Results}
\subsection{Experiment I: Single-event Gradient Diagnostic}
In the loss order \lone, \ltwo, \mss, \sot, \tfw, and \bicul, the fixed
renderer gives 60.0\%, 49.2\%, 42.5\%, 51.7\%, 75.8\%, and 100.0\%
target-directed descent, respectively. Thus \bicul{} is the only objective with
120/120 target-directed directions in this registered renderer and diagnostic
case; Fig.~\ref{fig:landscapes} does not imply a renderer-independent ranking.

\subsection{Experiment II: Phrase Recovery}
Table~\ref{tab:pilot-compact} reports all conditions. Figure~\ref{fig:lsd}
shows all LSD values, interquartile ranges, and medians. Both \bicul{} variants
achieved essentially perfect reconstruction for one- and two-event phrases.
Across all event counts, a \bicul{} variant has the lowest median pitch error.
A \bicul{} variant also has the lowest median onset error through four events, while the
published linear-Hz \tfw{} has the lowest median onset error at six and eight
events.

\section{Conclusion}
We introduced \bicul{} for direct pluck-onset and pitch recovery. Under the
fixed Fourier--Thiran renderer, it gives 120/120 target-directed directions;
reset smooths the plotted three-event DF2 sweeps. Across 6000 fits, \bicul{}
gives stronger joint recovery, with Log-Q further improving descriptive
results. At four events, Log-Q \bicul{}'s median pitch error (19.1 cents) is
comparable to reported JNDs~\cite{holmes2022pitch}, while its onset error
(6.75 ms) is below reported guitar-context timing JNDs~\cite{camara2026timing};
from six events, both errors exceed those perceptual scales, with pitch error
above one semitone. This contextual comparison does not establish perceptual
equivalence between JND experiments and phrase-level mean absolute errors.

Future work should test reset generality, expressive techniques, higher sample
rates, and persistent-state optimisation.

\clearpage
\section*{Acknowledgment}
Pablo Tablas de Paula is supported by the UKRI Centre for Doctoral Training in
Artificial Intelligence and Music (grant EP/S022694/1). This research utilised
Queen Mary's Apocrita HPC facility, supported by QMUL Research-IT
(\url{https://doi.org/10.5281/zenodo.438045}), using NVIDIA V100 and A100 GPUs.

\IEEEtriggeratref{13}
\bibliographystyle{IEEEtran}
\typeout{ICASSP bibliography database: \PaperRoot/references.bib}
\bibliography{\PaperRoot/references}
\end{document}
