# Overleaf source

Upload or link this directory as the Overleaf project. `main.tex`,
`IEEEtran.cls`, the bibliography, and every referenced figure/table are
self-contained below `paper/`; no parent-directory paths or generated build
files are required.

For a local build from the repository root, run `pixi run paper`.

