# Optional. Relative paths are resolved from this checkout, not the working directory.
# Existing environment variables and command-line options take precedence.
PHRASE_OUTPUT_DIR=results
PHRASE_CACHE_DIR=results/cache
PHRASE_LOG_DIR=results/logs
PHRASE_SLURM_PROFILE=generic
